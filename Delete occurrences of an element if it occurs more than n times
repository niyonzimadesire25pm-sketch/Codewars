function deleteNth(arr, n) {

  let result = [];
  let count = {};

  for (let i = 0; i < arr.length; i++) {

    let num = arr[i];

    count[num] = (count[num] || 0) + 1;

    if (count[num] <= n) {
      result.push(num);
    }

  }

  return result;
}
